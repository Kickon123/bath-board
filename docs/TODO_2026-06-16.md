# TODO 2026-06-16

## 1. 大浴場のデバイス名を確認
Inkbirdアプリのホーム一覧を開いて、大浴場デバイスの**正確な表示名**を確認する。

- 半角カッコ `大浴場(男)` か
- 全角カッコ `大浴場（男）` か
- まったく別の名前か

---

## 2. コード修正（B案を推奨）

**A案**: `config.phone.json` の `devices` を実際の名前に合わせる
- 単純だが、名前が変わるたびに直す必要あり

**B案**: `find_device_coords` を全角/半角無視のマッチに修正する ← 推奨
- 括弧の違いで今後もハマらない根本対処
- `adb_reader.py` の修正 → USB書き込み → `b restart`

---

## 3. 動作確認
- `https://bath-board.onrender.com/api/baths` で id=20(男湯)・id=21(女湯) の温度が取得できているか確認
- `https://bath-board.onrender.com/bath/all` で左右表示を確認
