# 作業ログ 2026-06-14

## 今日の作業概要

スライドショー表示の実装と調整、フォルダ整理、git管理の開始。

---

## Gitコミット履歴

| ハッシュ | 内容 |
|---|---|
| `b7eebb0` | スライドショー: 3Dカルーセル(縦回転・丸み・全体表示)＋7:3レイアウト |
| `01a2bc4` | ベース保存: スライドショー(左温度固定/右縦回転)＋クラウド中継システム完成版 |

---

## 主な作業内容

### 1. フォルダ整理
- `static/` の旧テストファイル（base1〜6.png、chatgpt*.html等）を削除
- `archive/` に旧設定・ツールファイルを移動（config.bluestacks.json、capture_map.py等）
- `docs/` にPDFを移動
- `bath_system.log*` 削除（起動時に再生成される）
- `latest.json`、`temperatures.json` はPCのテスト残骸（スマホ側が本番）

### 2. スライドショー実装（static/slideshow.html）

#### ベース版（01a2bc4）
- 左右50/50分割（左: 温度表示固定、右: 画像3枚ローテーション）
- 縦回転トランジション（rotateX）
- 10秒間隔
- 画像アスペクト比に合わせたコンテナ高さ

#### 現行版（b7eebb0）
- 左70% / 右30% レイアウト、両パネル90vh、下揃え
- 右パネル: 3Dカルーセル方式
  - 前面1枚＋上奥・下奥に2枚が常時見える
  - 縦方向（rotateX）に回転して切り替わり
  - border-radius: 40px（上下に丸み）
  - object-fit: contain（画像全体を縮小表示）
- ChatGPT生成画像3枚を使用:
  - `ChatGPT Image May 15, 2026, 08_26_50 PM.png`
  - `ChatGPT Image May 23, 2026, 11_16_35 PM.png`
  - `ChatGPT Image May 23, 2026, 11_26_28 PM.png`

### 3. git管理開始
- `git init` でリポジトリ初期化
- `.gitignore` 作成（ログ・一時ファイルを除外）
- 2コミットで現在の状態を記録

### 4. Renderに関する確認
- 無料プランはスリープ制あり（15分無アクセスで休止、30〜60秒で復帰）
- 月750時間制限（1サービスなら常時稼働可能）
- スマホが3分おきに送信していればスリープしない

---

## 現在のファイル構成（本番）

```
bath_system/
├── adb_reader.py       # 温度取得メイン
├── server.py           # ローカルFlaskサーバー
├── cloud_server.py     # クラウド用サーバー
├── run.sh              # スマホTermux起動スクリプト
├── config.phone.json   # スマホ用設定
├── config.json         # PC用設定（Windows ADB）
├── requirements.txt
├── static/
│   ├── slideshow.html      # メイン案内板
│   ├── base7-flat.html     # 温度表示単体
│   ├── base7.png           # 湯畑マップ画像
│   ├── bg.png              # 背景画像
│   ├── app.js / style.css
│   └── ChatGPT Image *.png # 静止画3枚
├── deploy_cloud/       # Render用デプロイファイル
├── docs/               # ドキュメント
└── archive/            # 旧ファイル保管
```

## 課題・メモ

- スマホ再起動後は `adb tcpip 5555` のためUSB接続が1回必要
- Termux:Boot で自動起動できる可能性あり（未検証）
- 温度表示左パネルの黒枠問題: マップ画像（横長）と縦長パネルのアスペクト比不一致が原因。現状は90vh設定で運用
